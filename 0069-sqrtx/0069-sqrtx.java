class Solution {
    public int mySqrt(int x) {
        long low = 1;
        long high = x;

        long val = x;
        long calc = 0;

        while(low < high) {
            long mid = low + (high - low)/2;

            calc = mid * mid;

            if(l) 
        }
    }
}