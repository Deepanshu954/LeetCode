class Solution {
    public int calPoints(String[] op) {
        int res = 0;

        for(int i = 0; i < op; i++) {
            if(op[i] == "+") {
                res += op[i - 1] + op[i - 2];
            }

            if(op == "D") {
                res += 2 * 
            }
        }
        
    }
}